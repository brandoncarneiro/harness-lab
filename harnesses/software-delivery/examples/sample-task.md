# Sample Software Task

Build a small validation fix in the signup flow.

The coding agent should inspect the relevant form and validation files, make the smallest safe change, avoid unrelated refactors, run the targeted tests if available, and return a summary of files changed, commands run, and risks.

Do not push, open a PR, or merge automatically.
